#include "main.h"

/**
 * _strlen - returns the length of a string.
 * @s: input stringeturn.
 * Return: length of a string.
 */
int _strlen(char *s)
{
	int _strlen(char *s)

		while (*(s + count) != '\0')
			count++;
		return (count);
}
